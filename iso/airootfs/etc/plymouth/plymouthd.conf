# SharkOS live ISO boot splash. The theme files are copied into the live
# airootfs by build-iso.sh; the `plymouth` mkinitcpio hook (also added there)
# reads this to bake the theme into the live initramfs.
[Daemon]
Theme=sharkos
ShowDelay=0
