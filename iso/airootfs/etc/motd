
  Welcome to the SharkOS live installer 🦈

  To install SharkOS, run:

      sharkos-install

  This installs a base Arch system. After rebooting, log in and run the
  SharkOS setup (it prints the exact command when stage 1 finishes).

  Networking: wired is automatic; for Wi-Fi use `iwctl`.

