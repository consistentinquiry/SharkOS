# Extra packages baked into the SharkOS live/installer environment.
# sharkos-install (v2) is a pacstrap pipeline, not archinstall:
#   gum        — interactive prompts in the installer
#   cryptsetup — LUKS setup (also in releng, pinned here for clarity)
#   plymouth   — branded boot splash on the live ISO itself
git
gum
cryptsetup
plymouth
