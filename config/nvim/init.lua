-- sharkOS Neovim — a full IDE for TypeScript, Python and Java.
-- Plugins: lazy.nvim. Theme: generated from the active sharkOS theme (see
-- lua/plugins/colorscheme.lua + config/themes/templates/nvim-colors.lua.tpl).
-- Requires Neovim >= 0.11 (uses vim.lsp.config / vim.lsp.enable).
require("config.options")
require("config.keymaps")
require("config.lazy")
