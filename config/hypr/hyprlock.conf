general {
    disable_loading_bar = true
    hide_cursor = true
    grace = 0
}

background {
    monitor =
    path = screenshot
    blur_passes = 4
    blur_size = 8
    noise = 0.0117
    contrast = 1.2
    brightness = 0.9
    vibrancy = 0.2
    vibrancy_darkness = 0.2
}

input-field {
    monitor =
    size = 340, 56
    outline_thickness = 2
    rounding = 14

    outer_color = rgba(ffffffcc)
    inner_color = rgb(000000)
    font_color = rgb(ffffff)
    check_color = rgba(ffffffcc)
    fail_color = rgba(eb4d4b, 0.9)
    capslock_color = rgba(ffbe3c, 0.9)

    dots_size = 0.28
    dots_spacing = 0.22
    dots_center = true
    fade_on_empty = false
    placeholder_text = <span foreground="##ffffffa6">Enter Password...</span>
    hide_input = false

    position = 0, -40
    halign = center
    valign = center
}

label {
    monitor =
    text = cmd[update:1000] echo "$(date +'%H:%M')"
    color = rgb(ffffff)
    font_size = 52
    font_family = JetBrainsMono Nerd Font

    position = 0, 80
    halign = center
    valign = center
}

label {
    monitor =
    text = cmd[update:1000] echo "$(date +'%A, %d %B')"
    color = rgb(ffffff)
    font_size = 18
    font_family = JetBrainsMono Nerd Font

    position = 0, 30
    halign = center
    valign = center
}
